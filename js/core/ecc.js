export function encodeECC(bits){let o='';for(let i=0;i<bits.length;i+=32){const b=bits.substr(i,32).padEnd(32,'0');o+=b+b+b;}return o;}export function decodeECC(bits){let o='';for(let i=0;i<bits.length;i+=96){const g=[bits.substr(i,32),bits.substr(i+32,32),bits.substr(i+64,32)];o+=g[0].split('').map((_,i)=>{let c=g.filter(b=>b[i]==='1').length;return c>=2?'1':'0';}).join('');}return o;}