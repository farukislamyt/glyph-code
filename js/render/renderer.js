import{drawGlyph}from'./glyph.js';export function renderFrame(bits,canvas){const ctx=canvas.getContext('2d'),chunk=6;const total=Math.ceil(bits.length/chunk);const grid=Math.ceil(Math.sqrt(total));const cell=canvas.width/grid;const pad=bits.padEnd(grid*grid*chunk,'0');ctx.fillStyle='white';ctx.fillRect(0,0,canvas.width,canvas.height);for(let i=0;i<pad.length;i+=chunk){const idx=i/chunk;drawGlyph(ctx,pad.substr(i,chunk),(idx%grid)*cell,Math.floor(idx/grid)*cell,cell);}}